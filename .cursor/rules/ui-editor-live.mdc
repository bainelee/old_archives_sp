---
description: 预制作 UI 的编辑器动态调整规范
globs:
  - "scripts/ui/**"
  - "scenes/ui/**"
alwaysApply: false
---

# 预制作 UI 编辑器规范

适用于**非游戏过程中自动生成**、而是在运行前**预先制作与排布**的 UI 组件。

## 原则

1. **编辑器中可动态调整**：所有需要在编辑器中检查效果的数值（尺寸、位置、颜色等）必须支持在 Inspector 中修改并**立即生效**，无需运行游戏；游戏运行后按调整后的状态工作。
2. **禁止将编辑器可见逻辑放在 _ready()**：凡需在编辑器中检查效果的功能，不得写在 `_ready()` 中。`_ready()` 仅在运行后执行，编辑器编辑状态下不会调用。

## 实现方式

- **数值配置**：使用 `@export` 暴露属性，配合 **setter** 在值变化时立即执行更新逻辑（如 `custom_minimum_size`、布局重算等）。
- **编辑器可见**：需要编辑器预览时，使用 `@tool`，并确保 setter 或属性变更逻辑在编辑状态下也会执行。
- **与 _ready() 的区分**：`_ready()` 仅用于运行期一次性初始化（如连接信号、获取运行依赖节点等），不用于依赖编辑器调整才能观察到的视觉效果或布局。

## 适用场景

- 预制作的按钮、面板、控件等可复用 UI 组件
- 在 .tscn 中手工排布的界面
- 需要设计时在编辑器中反复调参的 UI

## 不适用

- 游戏运行时动态生成的 UI（如战斗伤害数字、动态弹出的提示）
- 纯数据/逻辑初始化，无视觉可观察效果

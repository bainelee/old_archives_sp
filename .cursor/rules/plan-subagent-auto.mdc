---
description: Plan 相关请求必须自动启用 plan-subagent-auto 技能并生成子代理方案
alwaysApply: true
---

# Plan 自动子代理方案（强制）

当请求与 Plan 相关时，必须执行以下规则：

1. 立即启用项目技能：`.cursor/skills/plan-subagent-auto/SKILL.md`
2. 无论是新建 Plan 还是追加 Plan，输出中都必须包含 `子代理使用方案` 小节
3. 不得等待用户再次提醒后才补充子代理方案
4. 若计划被更新，子代理分工与并行关系也必须同步更新

## 视为 Plan 相关的典型表述

- “使用 plan 模式”
- “做一个计划 / 出个 plan”
- “给已有计划加一条要求”
- “更新方案 / 调整计划步骤”

## 失败判定

若 Plan 输出缺失 `子代理使用方案`，视为未满足需求，必须立即重写并补齐。

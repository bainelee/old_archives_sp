# Chat-First Stepwise 核心要求（强制）

以下要求为本项目测试流程的最高优先级约束，必须始终执行：

## 1) 播报渠道

- 测试流程步骤必须在 **Chat 对话界面** 播报。
- 不允许把 shell/terminal 输出当作主播报渠道。
- 若出现 shell 播报与 chat 播报冲突，以 chat 播报为准，并修正流程。

## 2) 低延迟

- 步骤播报必须尽可能低延迟。
- 优先使用服务端原子执行/自驱执行（如 `step_once` / `run_stepwise_autopilot`）以减少往返延迟。
- 禁止使用会导致历史补发、批量推送、明显延迟的路径作为主路径。

## 3) 每步固定五段协议（必须完整）

每个步骤都必须显示以下阶段，且顺序固定：

1. 即将开始
2. 开始执行
3. 执行结果
4. 验证结论
5. 通过后进入下一步 / 失败立即停止

## 4) 严格门控

- 只有验证通过才允许进入下一步。
- 任一步验证失败必须立即停止，不得继续执行后续步骤。

## 5) 执行原则

- 所有改动、脚本与工具设计都必须优先满足上述四条。
- 若实现方式与核心要求冲突，必须重构实现方式，而不是降低核心要求。
